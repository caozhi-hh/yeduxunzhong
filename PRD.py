# 旅游攻略生成器 — 产品需求文档（PRD）

## 1. 项目概述

**产品名称：** 穷游攻略生成器
**目标用户：** 大学生等预算有限的旅行爱好者
**核心痛点：** 手动做旅行攻略耗时长（查景点、比交通、算预算），需要一种快速生成个性化攻略的方式
**解决方案：** AI 自动生成完整的旅行攻略，支持多轮修改和 Word 导出

## 2. 功能需求

### 2.1 用户输入

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 出发城市 | 文本 | 用户所在城市 | 苏州 |
| 目的地 | 文本 | 目标城市 | 成都 |
| 天数 | 数字 | 旅行天数 | 4 |
| 预算上限 | 数字 | 总预算（元） | 3000 |
| 旅行类型 | 选择 | 风格偏好 | 特种兵 / 性价比 / 享受 / City Walk / 漫游 |

### 2.2 AI 输出

**第一轮：景点推荐**
- 列出目的地热门景点（含配图 + 门票价格）
- 用户勾选想去的景点（每日必去大景点）
- AI 根据用户选择安排游玩路线

**第二轮：完整攻略生成**
- 每日行程安排：
  - 景点（名称 + 游玩时长 + 门票价格 + 配图）
  - 交通（方式 + 费用，按旅行类型匹配）
  - 住宿（推荐区域 + 价格区间）
  - 餐饮（当地特色 + 人均消费）
- 预算分配表（交通/住宿/餐饮/门票各占多少）

### 2.3 多轮修改

用户可以对话式修改攻略，例如：
- "第二天太累了，调松一点"
- "把第三天的 XX 景点换成 YY"
- "预算超了，住宿降一档"
- "加一个都江堰一日游"

### 2.4 导出功能

- 支持将完整攻略导出为 Word 文档
- Word 中每个景点附带一张真实景点图片

## 3. 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Streamlit | 对话式交互界面 |
| 后端 | LangGraph | Agent 编排（Supervisor 多 Agent 模式） |
| LLM | Qwen-Turbo | 通义千问，通过 DashScope API 调用 |
| 搜索 | Tavily / SerpAPI | 获取最新景点、交通、价格信息 |
| 图片 | Unsplash API | 免费高质量景点图片 |
| 导出 | python-docx | 生成 Word 文档 |
| 记忆 | MemorySaver | 多轮对话历史（开发阶段） |

## 4. 用户体验

### 4.1 交互流程

```
用户填写表单（出发地/目的地/天数/预算/类型）
        ↓
AI 推荐热门景点（带配图 + 门票价格）
        ↓
用户选择想去的景点
        ↓
AI 生成完整攻略（行程 + 预算分配）
        ↓
用户对话式修改（不满意的地方）
        ↓
确认后导出 Word（含景点配图）
```

### 4.2 界面布局

- 左侧：参数设置（出发地、目的地、天数、预算、旅行类型）
- 右侧：对话区（AI 推荐 + 攻略展示 + 修改对话）
- 底部：导出按钮

### 4.3 预算分配逻辑

按旅行类型自动分配预算比例：

| 旅行类型 | 交通 | 住宿 | 餐饮 | 门票 |
|----------|------|------|------|------|
| 特种兵 | 硬座/夜车（30%） | 青旅/民宿（20%） | 路边摊（20%） | 景点为主（30%） |
| 性价比 | 高铁二等（30%） | 经济酒店（30%） | 当地特色（20%） | 精选景点（20%） |
| 享受 | 高铁一等/机票（25%） | 舒适酒店（35%） | 餐厅打卡（25%） | 深度体验（15%） |
| City Walk | 公交/地铁（15%） | 市中心民宿（40%） | 咖啡馆+小吃（25%） | 免费景点为主（20%） |
| 漫游 | 随意（20%） | 特色住宿（35%） | 慢食体验（30%） | 随缘（15%） |

## 5. 边界情况

| 情况 | 处理方式 |
|------|---------|
| 预算过低（如 200 元去西藏 7 天） | AI 提醒预算不足，建议调整天数或预算 |
| 搜索不到景点信息 | 使用 AI 自身知识补充，并标注"价格可能不准确" |
| 目的地无直飞/直通 | AI 推荐中转方案 |
| 用户要求不合理（如 1 天逛完北京所有景点） | AI 提醒时间不足，建议取舍 |

## 6. 项目结构（预期）

```
travel-planner/
├── app.py              # Streamlit 主程序
├── agent.py            # LangGraph Agent 定义
├── tools/              # 工具函数
│   ├── search.py       # 搜索工具（景点/交通/价格）
│   ├── images.py       # 图片搜索（Unsplash）
│   └── export.py       # Word 导出
├── requirements.txt    # 依赖
└── .env                # API 密钥
```
